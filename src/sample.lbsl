A = 3*4.
B = 5+2.
C = 3.
#show B.

while(A>0)
    begin
        A = A-1.
        #show A.
    end

if(C>2)
    begin
        #show C.
    end
elseif(C<2)
    begin
        #show C.
    end
else
    begin
        #show C.
    end

A = true.
#show A.